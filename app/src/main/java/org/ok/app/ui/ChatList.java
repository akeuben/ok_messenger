package org.ok.app.ui;

import javax.swing.*;

public class ChatList extends JPanel {
    public ChatList() {
        JButton newChatButton = new JButton("New Chat");
        add(newChatButton);
    }
}
